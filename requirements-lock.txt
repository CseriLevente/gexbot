# Resolved dependency versions, generated from pyproject.toml.
#
#   .venv/Scripts/python.exe -m pip install -e ".[dev]"
#   .venv/Scripts/python.exe -m pip freeze --exclude-editable | sort > requirements-lock.txt
#
# pyproject.toml holds compatible RANGES for the direct dependencies; this file
# pins what a known-good environment actually resolved to. Regenerate it
# deliberately, in a commit that says why.
#
# The engine core (src/gex, src/domain, src/synthetic) imports no *code* from
# this list. The config loader needs PyYAML and the real HTTP transport needs
# httpx; the rest is tooling.
#
# tzdata is the one runtime entry the core does reach, and it is data: the IANA
# timezone database, with no importable logic. v2.1.7 adopted it because the
# hand-written US Eastern rule it replaced could not represent the repeated hour
# of the autumn DST transition and returned an instant an hour wrong.
#
# Python 3.12 or 3.13.

anyio==4.14.2
certifi==2026.7.22
colorama==0.4.6
coverage==7.15.2
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
librt==0.13.0
mypy_extensions==1.1.0
mypy==1.20.2
packaging==26.2
pathspec==1.1.1
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
pytest-cov==7.1.0
PyYAML==6.0.3
ruff==0.14.14
tzdata==2025.3
types-PyYAML==6.0.12.20260724
typing_extensions==4.16.0
