.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Secrets and machine-local config. Never commit a vendor credential.
.env
.env.*
!.env.example
config/local.yaml
secrets/

# Runtime data and artefacts
data/
logs/
*.db
*.sqlite3
exports/
research_output/
# Raw vendor responses captured at runtime. Checked-in fixtures live under
# tests/fixtures/vendor/ and are NOT covered by this rule.
raw_responses/
# The configured capture destination. A real run writes vendor bytes here; a
# test run writes fixture bytes here. Neither belongs in the repository or in a
# release archive, and 573 of the latter reached a build before this line
# existed -- see tests/unit/test_release_integrity.py, which now fails on them.
artifacts/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Release archives are build output, produced by docs/RELEASE.md step 4.
# They must never be tracked: a tracked artefact makes the tree dirty at the
# exact moment the release procedure requires it to be clean.
gex-bot-*.zip

# Scratch files from the development loop. PowerShell here-strings break on this
# machine, so edits are applied by writing a throwaway ``_patch.py`` and commit
# messages by writing ``_commit_msg.txt`` -- and ``git add -A`` swept
# ``_commit_msg.txt`` into two v2.1.18 commits and then into the release archive
# before this line existed. A build somebody downloads should not contain the
# note explaining how it was built. See test_no_scratch_file_reaches_a_release.
/_*.py
/_*.txt
/_*.md
